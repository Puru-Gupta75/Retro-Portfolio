/**
 * This file is intentionally minimal.
 *
 * The project uses Tailwind CSS v4, which uses a CSS-first configuration
 * approach. All design tokens, content sources, and keyframes are defined
 * in app/globals.css via @theme and @source directives.
 *
 * See: https://tailwindcss.com/docs/v4-upgrade
 */
module.exports = {};
